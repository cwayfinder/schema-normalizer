import { ComponentDescription } from '../../types/component-description';

export const description: ComponentDescription = {
  traits: [],
  properties: [
    { key: 'html', type: 'string', htmlAllowed: true },
  ],
};

export const defaultSchema = {};
